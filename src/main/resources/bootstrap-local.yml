server:
  port: 6605
  db: mtcplat-job
spring:
  application:
    name: platform-job
  datasource:
    url: jdbc:mysql://${db.url:10.200.120.45}:${db.port:3306}/${server.db}?useSSL=false&characterEncoding=utf-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai
    username: ${db.user:db_service_test}
    password: ${db.pw:Q&kUJb=U84q_}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
eureka:
  instance:
    prefer-ip-address: true
    status-page-url-path: /page/index.html
  client:
    serviceUrl:
      defaultZone: http://10.200.120.48:6655/eureka/